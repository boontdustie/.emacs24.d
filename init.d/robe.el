;; As you change the code in your project,
;; you'll want to update the running process.
;; To load the current file, type C-c C-l (ruby-load-file)

;; When you're developing a Rails project, you can type C-c C-k instead,
;; to reload the whole environment at once.

;; https://github.com/dgutov/robe
(add-hook 'ruby-mode-hook 'robe-mode)
